# Configuraci\u00F3n de desarrollo para MongoDB Atlas
spring.application.name=franchise-api
server.port=${PORT:8081}

# MongoDB Atlas - desarrollo
spring.data.mongodb.uri=mongodb+srv://rickmartinezbanda_db_user:${DB_PASSWORD}@franchise-api-dev.rz2dpuc.mongodb.net/franchise-api-dev?retryWrites=true&w=majority&appName=franchise-api-dev

# Logging para desarrollo
logging.level.com.franchise.api=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.reactor.netty=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Actuator para desarrollo
management.endpoints.web.exposure.include=health,info,metrics,env
management.endpoint.health.show-details=always
management.health.mongo.enabled=true

# Configuraci\u00F3n de desarrollo
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always